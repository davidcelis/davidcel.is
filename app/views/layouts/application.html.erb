<!DOCTYPE html>
<html>
  <head>
    <title>davidcel.is</title>
    <meta name="description" content="Thoughts and musings of a Cowboy Coder.">

    <%= auto_discovery_link_tag(:atom, main_feed_url, title: "davidcel.is") %>
    <%= auto_discovery_link_tag(:atom, articles_feed_url, title: "davidcel.is - Articles") %>
    <%= auto_discovery_link_tag(:atom, notes_feed_url, title: "davidcel.is - Notes") %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://cdn.fontshare.com" crossOrigin="anonymous"/>
    <link rel="stylesheet" href="https://api.fontshare.com/v2/css?f[]=satoshi@700,500,400&display=swap"/>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/lightgallery@2.7.1/css/lightgallery-bundle.min.css" rel="stylesheet">

    <%= javascript_importmap_tags %>

    <link rel="icon" type="image/x-icon" sizes="48x48" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">

    <meta property="og:title" content="<%= content_for(:og_title) || "davidcel.is" %>">
    <meta property="og:description" content="<%= content_for(:og_description) || "Thoughts and musings of a Cowboy Coder." %>">
    <meta property="og:type" content="<%= content_for(:og_type) || "website" %>">
    <meta property="og:url" content="<%= content_for(:og_url) || request.original_url %>">
    <% if content_for?(:og_image) %>
      <%= content_for(:og_image) %>
    <% else %>
      <meta property="og:image" content="<%= image_url("me.jpg") %>">
      <meta property="og:image:alt" content="A photo of me on a horse, looking quite dapper.">
    <% end %>

    <meta name="twitter:card" content="<%= content_for(:twitter_card) || "summary" %>">
    <meta name="twitter:site" content="@davidcelis">
    <meta name="twitter:creator" content="@davidcelis">
    <meta name="twitter:title" content="<%= content_for(:og_title) || "davidcel.is" %>">
    <meta name="twitter:description" content="<%= content_for(:og_description) || "Thoughts and musings of a Cowboy Coder." %>">
    <% if content_for?(:twitter_image) %>
      <%= content_for(:twitter_image) %>
    <% else %>
      <meta name="twitter:image" content="<%= image_url("me.jpg") %>">
      <meta name="twitter:image:alt" content="A photo of me on a horse, looking quite dapper.">
    <% end %>

    <%= content_for(:head) %>
  </head>

  <body data-controller="local-time" class="bg-white antialiased">
    <%= render "header" %>

    <main class="border-t border-slate-200 lg:relative lg:ml-96 lg:border-t-0 xl:ml-[26rem]">
      <%= render "waveform" %>
      <%= render "navigation" %>

      <div class="relative">
        <div class="py-12 sm:py-8">
          <div class="lg:px-8">
            <div class="lg:max-w-4xl">
              <div class="mx-auto px-4 sm:px-6 md:max-w-2xl md:px-4 lg:px-0">
                <h1 class="text-2xl font-bold leading-7 text-slate-900"><%= content_for(:page_title) %></h1>
              </div>
            </div>
          </div>

          <div class="sm:mt-4 lg:mt-8 lg:border-t lg:border-slate-100">
            <%= yield %>
          </div>
        </div>
      </div>
    </main>

    <%= render "footer" %>
  </body>
</html>
