<article class="note py-10 sm:py-12">
  <div class="lg:px-8">
    <div class="lg:max-w-4xl">
      <div class="mx-auto px-4 sm:px-6 md:max-w-2xl md:px-4 lg:px-0">
        <div class="flex flex-col items-start">
          <section class="prose prose-slate prose-a:text-pink-500 hover:prose-a:text-pink-700 active:prose-a:text-pink-900 mt-1 text-base leading-7 text-slate-700 lg:prose-lg">
            <span class="select-none hidden md:block md:absolute md:-ml-6" title="Note">
              ✏️
            </span>

            <%= note.html.html_safe %>
          </section>

          <% if note.media_attachments.any? %>
            <%= media_gallery_tag(note.media_attachments) %>
          <% end %>

          <div class="mt-4 flex items-center gap-4">
            <%= link_to polymorphic_path(note), class: "flex items-center text-sm font-bold leading-6 link-primary" do %>
              <%= timestamp(note) %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</article>
